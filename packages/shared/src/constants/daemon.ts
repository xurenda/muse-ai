/** Daemon 默认监听地址 */
export const DEFAULT_DAEMON_HOST = '127.0.0.1'

/** Daemon 默认端口 */
export const DEFAULT_DAEMON_PORT = 7421

/** Web 开发环境访问 daemon 的代理前缀 */
export const DAEMON_PROXY_PREFIX = '/daemon'
