# Server 环境变量（复制为 .env 后按需修改）
PORT=3000
HOST=127.0.0.1

# Docker Compose 默认（packages/server/docker-compose.yml）
DATABASE_URL=postgresql://muse:muse@localhost:5432/muse
REDIS_URL=redis://localhost:6379
